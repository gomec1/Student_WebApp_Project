'use strict';

/**
 * notification-tierbesitzer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notification-tierbesitzer.notification-tierbesitzer');

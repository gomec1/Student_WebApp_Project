'use strict';

/**
 * notification-tierbesitzer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::notification-tierbesitzer.notification-tierbesitzer');
